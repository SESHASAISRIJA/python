def circular_rotate(lst, k):
    k = k % len(lst)
    return lst[-k:] + lst[:-k]

lst = list(map(int, input("Enter a list of numbers separated by space: ").split()))
k = int(input("Enter the value of k: "))
print(f"List after circular rotation: {circular_rotate(lst, k)}")
